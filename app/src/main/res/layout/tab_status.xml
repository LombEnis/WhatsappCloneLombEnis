<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/status_rec_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/text_floating_button"
        android:layout_width="43dp"
        android:layout_height="43dp"
        android:layout_marginBottom="15dp"
        android:backgroundTint="@color/dark_grey"
        android:clickable="true"
        android:src="@drawable/ic_edit_vector_23"
        android:tint="@color/white"
        app:borderWidth="0dp"
        app:fabCustomSize="43dp"
        app:layout_constraintBottom_toTopOf="@id/camera_floating_button"
        app:layout_constraintEnd_toEndOf="@id/camera_floating_button"
        app:layout_constraintStart_toStartOf="@id/camera_floating_button"
        app:maxImageSize="23dp" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/camera_floating_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="15dp"
        android:layout_marginBottom="15dp"
        android:backgroundTint="?attr/colorPrimary"
        android:clickable="true"
        android:src="@drawable/ic_camera_vector_23"
        android:tint="@color/white"
        app:borderWidth="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>